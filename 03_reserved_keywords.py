#  How many reserve keywords are there in python 
#  and why should we avoid using them as variable names.

from distutils.log import error


res_keywords = '''
 and       del       from      not       while    
as        elif      global    or        with     
assert    else      if        pass      yield    
break     except    import    print              
class     exec      in        raise              
continue  finally   is        return             
def       for       lambda    try
'''

We should not  use reserve words as variable names 
because it can throw error, it can bother during coding,
it is not consider as a good practice.